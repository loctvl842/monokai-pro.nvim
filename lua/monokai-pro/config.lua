local config = {
  transparent_background = true,
  italic_comments = true,
  filter = "pro",
  diagnostic = {
    background = true,
  },
  plugins = {
    bufferline = {
      underline_selected = true,
    }
  }
}

return config
